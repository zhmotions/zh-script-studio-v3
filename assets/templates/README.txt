Drop subtitle-ready .mogrt (Premiere/AE) or .ffx (After Effects) files in this folder.
Each one auto-appears as a built-in "default" template chip in the panel.

REQUIREMENT for auto text-fill: the .mogrt must expose its text layer's
"Source Text" property in the Essential Graphics panel before exporting.

File name = the chip name shown in the panel.
